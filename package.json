{
  "name": "llamaindex-lab",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "backend": ".\\backend\\venv\\Scripts\\python.exe -m uvicorn backend.app.main:app --reload --port 8000"
  }
}
